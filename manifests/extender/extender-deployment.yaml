apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbon-extender
  namespace: carbon-scheduler
  labels:
    app: carbon-extender
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbon-extender
  template:
    metadata:
      labels:
        app: carbon-extender
    spec:
      imagePullSecrets:
        - name: ghcr-auth
      containers:
        - name: extender
          image: ghcr.io/kevinauberson/k8s-carbon-aware-scheduler-extender/extender:sha-d99ec2513997b56489a480311696f50ff065ee19
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SIGNAL_FILE
              value: /etc/carbon-signal/signal.json
          volumeMounts:
            - name: carbon-signal
              mountPath: /etc/carbon-signal
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
      volumes:
        - name: carbon-signal
          configMap:
            name: carbon-signal
