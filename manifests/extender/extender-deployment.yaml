apiVersion: apps/v1
kind: Deployment
metadata:
  name: carbon-extender
  namespace: carbon-scheduler
  labels:
    app: carbon-extender
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carbon-extender
  template:
    metadata:
      labels:
        app: carbon-extender
    spec:
      imagePullSecrets:
        - name: ghcr-auth
      containers:
        - name: extender
          image: ghcr.io/kevinauberson/k8s-carbon-aware-scheduler-extender/extender:sha-20b7a3aba7a07385d08f4e7f1737986830d40b94
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SIGNAL_FILE
              value: /etc/carbon-signal/signal.json
          volumeMounts:
            - name: carbon-signal
              mountPath: /etc/carbon-signal
              readOnly: true
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
      volumes:
        - name: carbon-signal
          configMap:
            name: carbon-signal
