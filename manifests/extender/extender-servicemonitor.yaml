apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: carbon-extender
  namespace: carbon-scheduler
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: carbon-extender
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
